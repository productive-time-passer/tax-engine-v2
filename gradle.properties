# Use a Java runtime compatible with this project's Kotlin DSL setup.
# This avoids Gradle startup failures when the host default is Java 25+.
org.gradle.java.home=/root/.local/share/mise/installs/java/21.0.2
